**StatusDot** — small colored dot for inline status (system health, live indicators). `pulse` adds a soft ping.

```jsx
<StatusDot tone="emerald" pulse label="AI online" />
<StatusDot tone="rose" label="Stockout risk" />
```
