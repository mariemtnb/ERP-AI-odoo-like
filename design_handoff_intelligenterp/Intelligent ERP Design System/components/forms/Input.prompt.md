**Input** — labelled text field with emerald focus ring, optional leading icon, hint or error text.

```jsx
<Input label="Email" type="email" placeholder="you@company.com" icon={<MailIcon/>} />
<Input label="SKU" error="Already exists" />
```
