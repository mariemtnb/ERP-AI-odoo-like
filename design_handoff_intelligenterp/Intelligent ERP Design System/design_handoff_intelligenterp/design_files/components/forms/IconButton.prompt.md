**IconButton** — chromeless square button holding one icon. Use in toolbars, top bar, and table row actions.

```jsx
<IconButton aria-label="Edit"><PencilIcon/></IconButton>
<IconButton size="sm" aria-label="Delete"><TrashIcon/></IconButton>
```
Always give an `aria-label`.
