**Card** — primary surface container with soft elevation and a hairline highlight. Compose with `CardHeader`, `CardTitle`, `CardContent`. Pass `hover` for clickable cards (lift + glow).

```jsx
<Card hover>
  <CardHeader><CardTitle>Low stock</CardTitle></CardHeader>
  <CardContent>…</CardContent>
</Card>
```
