**Skeleton** — shimmering placeholder for loading states. Match the shape of the content it replaces.

```jsx
<Skeleton width={120} height={12} />
<Skeleton height={80} radius="var(--radius-lg)" />
```
